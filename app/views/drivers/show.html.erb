
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @driver.name %>
</p>

<p>
  <strong>Company name:</strong>
  <%= @driver.company_name %>
</p>

<p>
  <strong>Cell phone:</strong>
  <%= @driver.cell_phone %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @driver.phone %>
</p>

<p>
  <strong>Website:</strong>
  <%= @driver.website %>
</p>

<p>
  <strong>Address:</strong>
  <%= @driver.address %>
</p>

<p>
  <strong>Number of trucks:</strong>
  <%= @driver.number_of_trucks %>
</p>

<p>
  <strong>Non runners:</strong>
  <%= @driver.non_runners %>
</p>

<hr>
  Routes covered by <%= @driver.name %>
<hr>
<div>
  <%= link_to 'add a new Route', new_driver_route_path(@driver)%>
</div>

<table>
  <thead>
    <th colspan="">from</th>
    <th colspan="">To</th>
    <th colspan="2">Options</th>

  </thead>
  <tbody>
    <% @routes.each do |route| %>
    <tr>
      <td><%= route.city_from %>,<%= route.province_from %></td>
      <td><%= route.city_to %>,<%= route.province_to %></td>
      <td><%= link_to 'Edit', edit_driver_route_path(@driver.id, route.id)  %></td>
      <td><%= link_to 'Destroy', driver_route_path(@driver.id, route.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>    
    <% end %>    
  </tbody>
</table>

<br>
  
<hr>
 <h2> Orders </h2>
<hr>

<table>
  <thead>
    <th colspan="">Customer</th>
    <th colspan="">Reservation number</th>
    <th colspan="">Shipping From</th>
    <th colspan="">Shipping To</th>
    <th colspan="2">status</th>
    <th colspan="2">actions</th>

  </thead>
  <tbody>
    <% @orders.each do |order| %>
    <tr>
      <td><%= Customer.find(order.customer_id).name %></td>
      <td><%= order.reservation %></td>
      <td><%= order.shipping_from_city %>,<%= order.shipping_from_province %> </td>
          <td><%= order.shipping_to_city %>,<%= order.shipping_to_province %> </td>
      <td></td>
      <td></td>
    </tr>    
    <% end %>    
  </tbody>
</table>
<br>
<br>
<%= link_to 'Edit', edit_driver_path(@driver) %> |
<%= link_to 'Back', drivers_path %>
