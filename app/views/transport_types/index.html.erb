
<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-8">
    <h2>Transportation</h2> 
  </div>
  <div class="col-lg-4">
  <div class="title-action"  style="color: white;">
    <%= link_to 'New Type of transportation', new_transport_type_path, :class =>"btn btn-primary" %>
  </div>
  </div>
</div>


<style>
    label{

        display: block;
        float: right;
        margin-bottom: 20px;
        font-weight: 700;
        }
        #DataTables_Table_0_info{
          display: none;
          
        }
</style>

        <div class="wrapper wrapper-content animated fadeInRight">
            <div class="row">
              <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>Type of transportation service currently being offered</h5>                       
                    </div>
                  <div class="ibox-content">
                      <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover dataTables-example" >
                          <thead>
                            <tr>
                              <th>Name</th>
                              <th>Description</th>
                              <th>Status</th>
                              <th colspan="2"></th>
                            </tr>
                          </thead>
                          <tbody>
                             <% @transport_types.each do |transport_type| %>
                                <tr>
                                  <td><%= transport_type.name %></td>
                                  <td><%= transport_type.description %></td>
                                  <td><%= transport_type.status %></td>
                                  <td><%= link_to 'Edit', edit_transport_type_path(transport_type) %></td>
                                <td><%= link_to 'Destroy', transport_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                              <% end %>
                          </tbody>
                          <tfoot>
                            <tr>
                              <th>Name</th>
                              <th>Description</th>
                              <th>Status</th>
                              <th colspan="2"></th>
                            </tr>
                          </tfoot>
                        </table>
                      </div>
                  </div>
                </div>
              </div>
            </div>
          </div>



          <script>
        $(document).ready(function(){
            $('.dataTables-example').DataTable({
                pageLength: 25,
                responsive: true,
                dom: '<"html5buttons"B>lTfgitp',
                buttons: [
                    { extend: 'copy'},
                    {extend: 'csv'},
                    {extend: 'excel', title: 'ExampleFile'},
                    {extend: 'pdf', title: 'Transport type List', header: true},

                    {extend: 'print',
                     customize: function (win){
                            $(win.document.body).addClass('white-bg');
                            $(win.document.body).css('font-size', '10px');

                            $(win.document.body).find('table')
                                    .addClass('compact')
                                    .css('font-size', 'inherit');
                    }
                    }
                ]

            });

        });

    </script>

